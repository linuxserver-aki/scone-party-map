# scone-party-map

スコーンパーティー 会場マップ（単一HTML）。

- 公開先: GitHub Pages（main ブランチ / ルート）
- 用途: Notion の embed ブロックに貼って表示する
- `index.html` 1ファイルで完結（画像は base64 埋め込み、外部CDN・API不使用）

## 更新方法

`index.html` を差し替えて push すると、数十秒〜数分で公開ページに反映される。
